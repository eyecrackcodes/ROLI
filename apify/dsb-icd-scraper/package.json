{
  "name": "dsb-icd-scraper",
  "version": "1.0.0",
  "description": "Scrapes the ICD (Insurance Calls Direct) admin-calls-agents-report and emits per-agent Billable Leads (the source of truth for inbound leads delivered). Replaces the DSB Lead Tracker as the source for ib_leads_delivered.",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "start": "tsx src/main.ts",
    "start:dev": "apify run"
  },
  "dependencies": {
    "apify": "^3.1.0",
    "playwright": "^1.40.0",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
