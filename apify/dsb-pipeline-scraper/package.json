{
  "name": "dsb-pipeline-scraper",
  "version": "1.0.0",
  "description": "Scrapes DSB CRM Advanced Dashboard Stats to extract pipeline compliance metrics (past due, new leads, call queue, follow-ups, post-sale) per agent.",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "start": "tsx src/main.ts",
    "start:dev": "apify run"
  },
  "dependencies": {
    "apify": "^3.1.0",
    "playwright": "^1.40.0",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
