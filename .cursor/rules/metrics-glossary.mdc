---
description: Definitions and formulas for all ROLI metrics
alwaysApply: true
---

# Metrics Glossary

## Production Metrics (from daily_scrape_data)

| Metric | Formula | Notes |
|---|---|---|
| Contact Rate | `(answered_calls / total_calls) * 100` | % of dials that were answered |
| Close Rate | `(apps / answered_calls) * 100` | % of answered calls that became apps |
| Dial Yield | `apps / total_calls` | Raw efficiency: apps per dial |
| Talk Time | Cumulative seconds on calls | Display as MM:SS |

## Leads Pool Metrics (from leads_pool_daily_data)

| Metric | Formula | Notes |
|---|---|---|
| Pool Efficiency | `pool_talk_time_min / pool_dials` | Minutes per dial — key gaming indicator |
| Assign Rate | `self_assigned_leads / answered_calls × 100` | % of answered calls the agent self-assigned as leads |
| Pool Close Rate | `sales_made / self_assigned_leads × 100` | Conversion rate on pool leads the agent self-assigned |
| Gaming Flag | Based on Pool Efficiency thresholds | See below |

### T3 Outbound KPI Targets (v2 — Three-Channel Model)

Daily compliance scorecard for T3 outbound agents across all three activity channels
(follow-up appointments, queue dialer, leads pool). Agent must pass 5 of 7 gates to be compliant.

| # | Gate | KPI | Target |
|---|---|---|---|
| 1 | Combined Volume | Total dials (queue + pool) | ≥ 200/day |
| 2 | Pool Presence | Pool % of total dials | 25-45% |
| 3 | Engagement | Long Calls (15+ min) | ≥ 4/day |
| 4 | Talk Time | Total talk minutes (queue + pool) | ≥ 180 min |
| 5 | Assignment | Self-assign rate | ≥ 30% of pool answered |
| 6 | Appointment Discipline | Past due follow-ups | 0 |
| 7 | Queue Health | Call queue size | ≤ 120 leads |

### Activity Channel Definitions

- **Follow-up appointments** — Task-date driven callbacks the agent scheduled. NOT in the dialer.
  Agent makes 1 attempt at scheduled time. If no answer, sticky note + move lead to queue for
  dialer cadence. Past due = missed or unworked appointments.
- **Queue dialer** — Agent's personal assigned leads. System cycles through automatically. Each lead
  gets max 6 contact attempts, then must be withdrawn. DNC/no-longer-reachable leads withdrawn immediately.
  Healthy queue: 50-120 leads. Queue > 150 = bloat. Contact attempts and withdrawals are queue-only
  mechanics — they do not apply to the leads pool.
- **Leads pool** — Shared unassigned leads. Agent enters pool session, dials, self-assigns every
  answered contact to remove from rotation. Self-assigning is the only way a lead leaves the pool.
  Feeds tomorrow's follow-up pipeline and queue.

Expected funnel at 200 combined dials (~115 queue + ~5 follow-up + ~80 pool): ~40 pool answered (50% contact rate) → 12+ self-assigned (30%) → 4+ long calls → 0.86 sales/day (at balanced 25-44% pool ratio). Avg premium per sale: $1,150.

### Presentation Rate (Pool)

| Metric | Formula | Notes |
|---|---|---|
| Presentation Rate | `long_calls / self_assigned_leads × 100` | % of self-assigned leads that resulted in a 15+ min presentation |

| Pres Rate | Interpretation |
|---|---|
| ≥ 20% | Strong — converting contacts into real pitch conversations |
| 12–19% | Acceptable — room for improvement on qualifying |
| < 12% | Weak — assigning but not engaging in meaningful conversations |

### Assign Rate Thresholds

All pool leads are unassigned. Agents should self-assign every answered contact — including
not interested and DNC — to remove leads from rotation and prevent unnecessary repeat calls.

| Assign Rate | Interpretation |
|---|---|
| ≥ 65% | Excellent — fully cleaning pool contacts |
| 30–64% | Compliant — pool is moving, discipline in place |
| < 30% | Non-compliant — contacts recycling back into pool |

### Queue Health Thresholds

Queue leads get a max of 6 contact attempts via the dialer, then must be withdrawn.
Follow-up appointments are task-date driven (NOT in the dialer queue).

| Queue Size | Interpretation |
|---|---|
| < 50 | Light — increase pool time for more self-assigns |
| 50–120 | Healthy — balanced inflow/outflow at 6-attempt cadence |
| 120–200 | Accumulating — audit queue, enforce withdrawals |
| > 200 | Bloated — leads rotting, stop pool until queue is cleared |

| Past Due | Interpretation |
|---|---|
| 0 | Clean — all appointments worked on schedule |
| 1–5 | Attention — catch up immediately |
| > 5 | Non-compliant — missing scheduled appointments, stop pool |

### Pool Efficiency Thresholds

| Efficiency | Interpretation |
|---|---|
| > 1 min/dial | Too slow — distracted, over-noting, idle time |
| 0.5–1 min/dial | Healthy, realistic workflow |
| 0.3–0.5 min/dial | Fast but acceptable |
| < 0.3 min/dial | Likely gaming / rapid skipping |

## Pipeline Metrics (from pipeline_compliance_daily)

| Metric | Formula | Notes |
|---|---|---|
| Total Stale | `past_due + new_leads + (call_queue * staleRate)` | staleRate: T1=15%, T2=10%, T3=8% |
| Revenue at Risk | `total_stale * avgPremium` | Uses agent's 30-day rolling avg premium (falls back to tier avg) |
| Projected Recovery | `total_stale * closeRate * avgPremium` | Uses agent's 30-day rolling close rate (falls back to tier avg) |
| Pipeline Health Score | Composite of 4 sub-scores (0-25 each) | See pipelineIntelligence.ts |
| Past Due Delta (d/d) | `today_past_due - yesterday_past_due` | Negative = improving, positive = deteriorating |

### Revenue Model Data Sources

Agent-level stats require minimum 3 days of data and 2+ sales (premium) or 5+ leads (close rate).
Below threshold, falls back to tier-level averages computed from same 30-day window.
Fallback constants when no data: T1=$400/8%, T2=$300/6%, T3=$1,150/4%.

## Follow-Up Compliance

| Metric | Formula | Notes |
|---|---|---|
| F/U Compliance % | `(1 - pastDue / (pastDue + todaysFollowUps)) * 100` | Point-in-time ratio |
| Past Due Delta | `today.pastDue - prior_day.pastDue` | d/d trend indicator |

If both pastDue and todaysFollowUps are 0, compliance = 100% (clean pipeline or no assignments).
The delta is the more actionable signal — a rising past-due count regardless of ratio means deterioration.

## Agent Performance Funnel (from agent_performance_daily)

| Metric | Definition | Notes |
|---|---|---|
| Dials | Total outbound dials | Self-explanatory |
| Leads Worked | Unique leads agent worked in the period | Each lead counts once per agent |
| Contact Made | Leads that hit "contact made" status as first occurrence | CRM status change, not just call answered |
| Conversations | Calls between 2 min and < 15 min | Duration bucket — mutually exclusive with Presentations |
| Presentations | Calls 15 min or more | Duration bucket — mutually exclusive with Conversations |
| Follow Up (Appt Set) | Follow-up appointments set | Self-explanatory |
| Sale Made | Sales closed | Self-explanatory |
| Talk Min | Total talk time in minutes | Self-explanatory |

### Funnel Conversion Rates

| Rate | Formula | Notes |
|---|---|---|
| Contact % | `contactsMade / leadsWorked × 100` | What % of worked leads were meaningfully contacted |
| Contact to Close % | `sales / contactsMade × 100` | Overall sales efficiency from contact |
| Conversation to Close % | `sales / conversations × 100` | Close rate from 2-15 min calls |
| Presentation to Close % | `sales / presentations × 100` | Close rate from 15+ min calls |

**Critical**: Conversations and Presentations are **parallel duration buckets**, NOT sequential funnel stages.
A call is either a Conversation (2-15 min) OR a Presentation (15+ min), never both.
All close % rates are calculated as `sales / [stage]`, not stage-to-stage.

## Velocity & Intraday Pace (from intraday_snapshots)

Velocity = whether an agent is on track to hit daily SOP targets by end of day.
Computed from cumulative `intraday_snapshots` data (written hourly by the n8n scrape).

### Pace Curve (CST)

Production day is 9 AM – 5 PM Central (8 hours). Agents typically ramp up in the
first hour and dip at lunch. The curve is NOT linear — it uses observed production
patterns to set realistic hourly expectations.

| Hour | Cumulative % | Expected Dials (of 200) | Expected Talk (of 180m) |
|---|---|---|---|
| 9 AM | 5% | 10 | 9m |
| 10 AM | 15% | 30 | 27m |
| 11 AM | 28% | 56 | 50m |
| 12 PM | 42% | 84 | 76m |
| 1 PM | 50% | 100 | 90m |
| 2 PM | 65% | 130 | 117m |
| 3 PM | 78% | 156 | 140m |
| 4 PM | 92% | 184 | 166m |
| 5 PM | 100% | 200 | 180m |

### Intraday Metrics Tracked

| Metric | Daily Target | Source |
|---|---|---|
| Combined Dials | 200 | total_dials + pool_dials |
| Talk Time | 180 min | talk_time_minutes + pool_talk_minutes |
| Long Calls | 4 | pool_long_calls |
| Pool Dials | 50 | pool_dials |

### Behind Pace Definition

An agent is **behind pace** when actual < 80% of expected for that hour.
An agent is **critical** when behind on 3 or more of the 4 tracked metrics.

Example: At 2 PM, expected combined dials = 200 × 0.65 = 130.
Behind threshold = 130 × 0.80 = 104. Agent with 103 dials → behind. Agent with 105 → on pace.

The 80% buffer prevents false alerts from minor timing variations (bathroom break,
long presentation call, etc.).

## Contact Rate vs Connect Rate vs Contact %

- **Contact Rate** = production dials answered (daily_scrape_data)
- **Connect Rate** = pool leads meaningfully contacted (leads_pool_daily_data)
- **Contact %** = leads that hit contact-made status / leads worked (agent_performance_daily)
- These are three distinct metrics from three different reports — never conflate them
